ktor {
    deployment {
        host = 0.0.0.0
        port = 5000
    }
    application {
        modules = [ com.licenta.ApplicationKt.module ]
    }
}
jwt {
    issuer = "http://0.0.0.0:$PORT"
    domain = "http://0.0.0.0:$PORT"
    audience = "users"
    realm = "ktor sample app"
}

database {
    url = "jdbc:mysql://localhost:3306/posture-improver"
    user = "root"
}